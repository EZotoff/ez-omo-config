---
description: Copy project path and session title to clipboard (no LLM round-trip)
---

Handled by plugin. This command copies `Project <path>:<branch>; Session <title>` to the clipboard directly.
