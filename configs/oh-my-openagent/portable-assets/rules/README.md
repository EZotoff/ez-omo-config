# Portable Rules

Place versioned Oh-My-OpenAgent rule fragments here when they are portable across machines.

Do not store live `.omo` runtime state, generated files, caches, logs, or secrets in this directory.
