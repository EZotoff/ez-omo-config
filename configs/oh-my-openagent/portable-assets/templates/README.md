# Portable Templates

Place versioned Oh-My-OpenAgent template fragments here when they are portable across machines.

Do not store live `.omo` runtime state, generated files, caches, logs, or secrets in this directory.
